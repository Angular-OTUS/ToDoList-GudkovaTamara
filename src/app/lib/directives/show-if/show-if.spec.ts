import { ShowIfDirective } from './show-if';

describe('ShowIf', () => {
  it('should create an instance', () => {
    const directive = new ShowIfDirective();
    expect(directive).toBeTruthy();
  });
});
