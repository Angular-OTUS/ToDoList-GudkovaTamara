import { TooltipComponent } from '@angular/material/tooltip';

describe('Tooltip', () => {
  it('should create an instance', () => {
    const directive = new TooltipComponent();
    expect(directive).toBeTruthy();
  });
});
